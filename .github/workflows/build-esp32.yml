name: Build Device Image

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'Target Board XIAO_ESP32C3 with Graphics'
        required: true
        default: 'XIAO_ESP32C3'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build Firmware
        uses: nanoframework/nanoframework-images-action@main
        with:
          target: ${{ github.event.inputs.target }}
          # Wir geben explizit an, wo die Cloud-Build-Ressourcen liegen
          branch: 'main'
